<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>You were tagged</title>
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.5; color: #111827; }
      .container { max-width: 620px; margin: 0 auto; padding: 20px; border: 1px solid #e5e7eb; border-radius: 8px; background: #f9fafb; }
      .title { font-size: 22px; font-weight: 700; margin-bottom: 12px; }
      .copy { font-size: 15px; margin-bottom: 14px; }
      .preview { font-size: 14px; color: #374151; background: #ffffff; border: 1px solid #e5e7eb; border-radius: 6px; padding: 12px; margin-bottom: 16px; }
      .button { display: inline-block; padding: 10px 16px; background: #2563eb; color: #ffffff !important; text-decoration: none; border-radius: 6px; font-weight: 600; }
      .footer { margin-top: 16px; font-size: 13px; color: #6b7280; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="title">You were tagged</div>
      <p class="copy">
        Hi {{recipientName}},
        <strong>{{actorName}}</strong>
        tagged you in
        {{itemLabel}}.
      </p>
      {{#if previewText}}
        <div class="preview">{{previewText}}</div>
      {{/if}}
      <p>
        <a href="{{itemUrl}}" class="button">Open item</a>
      </p>
      <p class="footer">This is an automated notification.</p>
    </div>
  </body>
</html>
