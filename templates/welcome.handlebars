<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      Welcome to
      {{#if appName}}
        {{appName}}
      {{else}}
        {{#if appUrl}}
          {{appUrl}}
        {{else}}
          your new workspace
        {{/if}}
      {{/if}}
    </title>
    <style>
      :root {
        color-scheme: light;
      }
    </style>
  </head>
  <body
    style="
      margin: 0;
      padding: 32px 16px;
      background: #f5f7fb;
      font-family: 'Segoe UI', Arial, sans-serif;
      color: #0f172a;
    "
  >
    <table
      role="presentation"
      cellpadding="0"
      cellspacing="0"
      width="100%"
      style="max-width: 640px; margin: 0 auto;"
    >
      <tr>
        <td>
          <table
            role="presentation"
            cellpadding="0"
            cellspacing="0"
            width="100%"
            style="
              background: #0f172a;
              color: #e2e8f0;
              border-radius: 18px 18px 0 0;
              padding: 28px 28px 20px;
            "
          >
            <tr>
              <td style="font-size: 12px; letter-spacing: 0.08em; text-transform: uppercase; color: #94a3b8; padding-bottom: 6px;">
                {{#if appName}}
                  {{appName}}
                {{else}}
                  {{#if appUrl}}
                    {{appUrl}}
                  {{else}}
                    Workspace Access
                  {{/if}}
                {{/if}}
              </td>
            </tr>
            <tr>
              <td style="font-size: 24px; font-weight: 700; line-height: 1.3; padding-bottom: 4px;">
                Welcome aboard
                {{#if name}}{{name}}{{else}}there{{/if}}!
              </td>
            </tr>
            <tr>
              <td style="font-size: 15px; color: #cbd5e1;">
                Your workspace is ready. Here is everything you need to get started.
              </td>
            </tr>
          </table>

          <table
            role="presentation"
            cellpadding="0"
            cellspacing="0"
            width="100%"
            style="
              background: #ffffff;
              border: 1px solid #e2e8f0;
              border-top: none;
              border-radius: 0 0 18px 18px;
              box-shadow: 0 16px 40px rgba(15, 23, 42, 0.08);
              padding: 28px;
            "
          >
            <tr>
              <td style="font-size: 15px; line-height: 1.7; color: #0f172a; padding-bottom: 12px;">
                You now have access to
                {{#if appName}}
                  {{appName}}
                {{else}}
                  {{#if appUrl}}
                    {{appUrl}}
                  {{else}}
                    your new workspace
                  {{/if}}
                {{/if}}.
                Sign in to explore everything available to you.
              </td>
            </tr>

            {{#if loginUrl}}
              <tr>
                <td style="padding: 6px 0 18px;">
                  <a
                    href="{{loginUrl}}"
                    style="
                      display: inline-block;
                      padding: 12px 22px;
                      background-color: #2563eb;
                      color: #ffffff;
                      text-decoration: none;
                      border-radius: 10px;
                      font-weight: 700;
                      font-size: 15px;
                      box-shadow: 0 10px 30px rgba(37, 99, 235, 0.25);
                    "
                  >
                    Open workspace
                  </a>
                </td>
              </tr>
              <tr>
                <td style="font-size: 13px; color: #475569; padding-bottom: 16px;">
                  If the button does not work, copy and paste this link into your browser:
                  <br />
                  <a href="{{loginUrl}}" style="color: #2563eb; word-break: break-all;">{{loginUrl}}</a>
                </td>
              </tr>
            {{/if}}

            <tr>
              <td
                style="
                  font-size: 14px;
                  color: #0f172a;
                  border: 1px solid #e2e8f0;
                  border-radius: 12px;
                  padding: 14px 16px;
                  background: #f8fafc;
                "
              >
                <strong style="display: block; font-size: 13px; color: #475569; text-transform: uppercase; letter-spacing: 0.04em; margin-bottom: 6px;">
                  Sign-in email
                </strong>
                <span style="font-size: 15px; font-weight: 700;">{{email}}</span>
              </td>
            </tr>

            <tr>
              <td style="font-size: 14px; color: #475569; padding-top: 18px;">
                Need help? Reply to this email and our team will assist you.
              </td>
            </tr>

            <tr>
              <td style="font-size: 14px; color: #0f172a; padding-top: 22px;">
                Thanks,
                <br />
                {{#if appName}}
                  {{appName}} Team
                {{else}}
                  {{#if appUrl}}
                    {{appUrl}} Team
                  {{else}}
                    The Team
                  {{/if}}
                {{/if}}
              </td>
            </tr>
          </table>
        </td>
      </tr>
    </table>
  </body>
</html>
